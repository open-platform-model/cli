# Tasks: platform-pull

Two sections per design.md. design.md carries no unverified assumption: the operator's status shape and the generator entry mapping were read at the pinned versions, and the CLI already runs the generator, so section 1 is not a spike.

## 1. The cluster arm reads the effective registry (internal/platform, internal/workflow/render, callers)

- [x] 1.1 In `internal/platform/cluster.go`, replace `ClusterSpecGetter` with `ClusterPlatformGetter` returning `*ClusterPlatform{Name, Generation, Spec, Status}` per design.md § The getter returns the document; NotFound and Forbidden stay warn-fallback. Verify: `cluster_test.go` reads a document with spec and status through the fake dynamic client, and the two fallback tests pass unchanged in meaning.
- [x] 1.2 In `internal/platform/spec.go`, add `Effective` and `DecodeCR` per design.md § Decoding the effective registry, keeping `DecodeCRSpec`'s legacy tolerance for the spec half and refusing a status row without a version. Verify: `spec_test.go` covers a status with subscription and registration rows, a status without a registry, a Ready=False condition, and the empty-version refusal.
- [x] 1.3 In `internal/platform/resolve.go`, generate from the effective entries when present, else the spec; add `RegistryOrigin` and `PackageIdentity` to `Resolution`, extend `Describe()`, raise the stale-generation and not-Ready warnings, and add `ResolveOptions.NoLocalFallback` (an unavailable CR returns a not-found error instead of falling back). Verify: `resolve_test.go` covers effective-wins (both entries in the generated `cue.mod`), spec-fallback provenance, the two warnings, `NoLocalFallback`, and the existing cluster and fallback tests updated to the new getter shape without weakening an assertion.
- [x] 1.4 Move the callers: `render.ModuleOpts`/`InstanceOpts.ClusterPlatform` to the new type, `resolvePlatformEnv` unchanged in behaviour, and `platform.ClusterPlatformGetterFor(k8sClient.Dynamic)` in `module apply`, `instance apply` and `instance diff`. Verify: `go build ./... && go vet ./...` pass and the render workflow tests are green.
- [x] 1.5 `task fmt lint test:unit` green, then commit `feat(platform): resolve the cluster platform from its effective registry`.

## 2. opm platform pull (internal/cmd/platform)

- [ ] 2.1 Add `internal/cmd/platform/pull.go`: `pull <dir>` with `--force` and the kubeconfig/context flags the apply commands use, a package-level runner taking the getter and `ModFiles` so tests inject both, `Resolve` with `NoLocalFallback`, the copy of the cached module into `<dir>` (refuse non-empty without `--force`), and the report per design.md § opm platform pull. Update the group's `Long` in `platform.go`: `check` is offline, `pull` reads the cluster. Verify: `opm platform --help` lists `pull`; `opm platform pull --help` documents the flag and the exit codes.
- [ ] 2.2 Tests in `pull_test.go` with the fake dynamic client and the fixture `ModFiles` graph: a pull writes a module whose `cue.mod` pins every status entry and whose files equal the cached module's; pulling twice yields identical bytes; a non-empty target without `--force` exits 1 and writes nothing; `--force` replaces; no Platform exits 5 naming the CR; a legacy spec exits 2 with the generation hint; the report names generation, package identity, operator version and each entry's source. Verify: `go test ./internal/cmd/platform/...` passes.
- [ ] 2.3 Cross-cutting: `task check` in full (including `openspec:check`); run the operator-owned e2e (`tests/e2e/instance_operator_owned_test.go`) against the dev cluster when one is available, or record that it was skipped, since the cluster arm's provenance line changed. Verify: green, or the skip is noted in the PR.
- [ ] 2.4 `task check` green, then commit `feat(cmd): add opm platform pull`.
