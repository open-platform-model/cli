# Tasks: cli-cr-inventory-backend

## 1. Foundations (CUE bump + CR store)

- [x] 1.1 Bump `cuelang.org/go` to v0.17.1 in `go.mod`; `task check` green (design D9; trial-verified — expect zero code changes) — landed on main independently (go.mod: v0.17.1; lint fixes in internal/cuetidy + pkg/render); branch rebased, duplicate commits dropped as upstream
- [x] 1.2 Add an integration fixture proving the loader honors `cue.mod/local-module.cue` `replaceWith` (local dir replacing a registry path, including a never-published path); fix loader resolution if it bypasses the SDK's module resolution — `pkg/loader/local_module_resolution_test.go`: builds a hermetic temp module graph (isolated `CUE_CACHE_DIR`, dep with `v` omitted) and proves `LoadInstanceFile` resolves a never-published `replaceWith` onto a local dir with no registry (0.007s). No loader fix needed — it already uses CUE's standard `load.Instances`.
- [x] 1.3 Define the `ModuleInstance` GVR/kind constants in `internal/inventory`; replace `internal/operator/uninstall.go`'s private `moduleInstanceGVR` with the shared import (design D1) — `internal/inventory/cr.go`; operator now imports `inventory.ModuleInstanceGVR`/`GroupOpmodel` (uninstall.go, rbac.go via local aliases)
- [x] 1.4 Implement entry↔CRD-wire-shape mapping (`group/kind/namespace/name/v/component`; `inventory.revision/digest/count/entries`) with round-trip unit tests (design D2) — `internal/inventory/wire.go` + `wire_test.go` (version→`v`, omitempty parity, int64 counters, float64 tolerance)
- [x] 1.5 Implement the CR store in `internal/inventory`: GET (NotFound ⇒ no inventory), SSA spec apply, SSA status-subresource apply, delete (NotFound ⇒ success), list + `status.instanceUUID` matching — all `unstructured` via the dynamic client, manager `opm-cli` (design D3) — `internal/inventory/store.go` + `store_test.go` (read-mapping/list/find/delete covered; SSA write path validated in e2e 6.1, fake dynamic client has no SSA create)

## 2. Ownership and provenance

- [x] 2.1 Implement the single ownership mode-resolution function (`spec.owner` → mode) replacing `EnsureCLIMutable`/`createdBy`; unit-test all owner values (design D4) — `inventory.ResolveOwnership` + `ownership_test.go`; consumed by apply and delete
- [x] 2.2 Loader records render provenance (main-module-local, or any local-path `replaceWith` in the main module's `local-module.cue`); apply stamps/omits `module-instance.opmodel.dev/source: local` accordingly; test SSA removal on registry re-apply (design D7) — `pkg/loader/provenance.go` (`ModuleRootFrom`, `HasLocalModuleReplacement`, unit-tested); render sets `Result.SourceLocal` (module apply = always local; instance apply = local-module.cue replaceWith detection, replacing the old `Version==""` heuristic); apply stamps/omits the annotation via `SpecInput.SourceLocal`. SSA field-ownership removal-on-omit is validated in the e2e path (fake dynamic client has no SSA); the doc-building/read-back is unit-tested.
- [x] 2.3 Extract `status.instanceUUID` from rendered `module-instance.opmodel.dev/uuid` labels (first non-empty, omit when absent) with unit tests (design D3) — `inventory.ExtractInstanceUUID` + `instanceuuid_test.go`

## 3. Pre-apply gate battery

- [x] 3.1 CRD presence gate with the `opm operator install --crds-only` hint (design D5.1) — `inventory.GateCRDPresent` + `gates_test.go`
- [x] 3.2 CRD field-presence floor (`spec.owner` + `status.inventory` in the served storage-version schema), unit-tested against the embedded B2 CRD manifest (design D5.2) — `inventory.GateCRDFieldFloor` + tests (against a synthesized CRD mirroring the B2 schema shape)
- [x] 3.3 Operator-version ceiling reading `Platform.status.operatorVersion`: absent ⇒ skip; RBAC-denied read ⇒ skip with warning; dev build ⇒ skip with warning; semver-older CLI ⇒ refuse (design D5.3) — `inventory.GateOperatorVersionCeiling` (golang.org/x/mod/semver) + tests
- [x] 3.4 `SelfSubjectAccessReview` pre-flight for `patch moduleinstances/status` (CLI-executor mode only), aborting before any apply with the `--rbac` remedial hint (design D5.5) — `inventory.GateStatusRBAC` + tests (fake authorization reactor)
- [x] 3.5 Wire the gate battery into `internal/workflow/apply` in order (gates → ownership → SSAR → existence check), dry-run exempt; integration-test gate ordering guarantees zero writes on failure — ordering coded in `apply.Execute`/`RunClusterGates`; per-gate unit tests done; the dedicated zero-writes-on-failure integration assertion is folded into e2e 6.3

## 4. Apply/delete/status/list/diff rewire

- [x] 4.1 Rewire `internal/workflow/apply`: previous inventory from the CR, spec write (owner/module/values — canonical declared reference for local applies), stale set + prune via existing logic, status subset write after prune (no conditions), dry-run untouched — `apply.go` (spec via `Module.CanonicalModuleRef()`, values via new `render.Result.Values`); `pkg/module.ModuleMetadata.CanonicalModuleRef` + `module_test.go`
- [x] 4.2 One-time Secret→CR migration in the apply path: legacy lookup, revision continuation, delete-Secret-after-status-write, idempotent re-run (design D6) — `inventory/legacy.go` (`FindLegacySecretInventory`/`DeleteLegacySecret`) + apply wiring (`LoadPreviousInventory`/`WriteInstanceRecord`/`cleanupLegacySecret`); revision continuity + idempotent-delete covered in `apply_test.go`
- [x] 4.3 Rewire `internal/workflow/query` (status/diff/list health): inventory resolution and per-entry discovery read the CR record; re-home `discover.go` input types — `query/status.go`, `query/list.go`, `inventory/discover.go` re-typed to `*Record`; `inventory_test.go` rewritten to seed CRs. Also updated `kubernetes/status.go` FormatStatus warning from the stale `Owner == "controller"` to operator-owned (`!= "" && != "cli"`), with unit tests
- [x] 4.4 Rewire `instance delete`: ownership refusal with kubectl hint, prune from CR entries, delete CR last (replace `InventorySecretName` ordering in `internal/kubernetes/delete.go`) — `cmd/instance/delete.go` (`ensureDeleteAllowed` → `ResolveOwnership`, CR deleted last by the command); `kubernetes/delete.go` drops the Secret-last block for `InventoryRecordExists`
- [x] 4.5 `instance list`: namespace-scoped CR list + `--all-namespaces`/`-A` cluster-wide list with actionable RBAC error; sorted by name; corrupt CRs warned and skipped — `inventory.ListRecords` + `cmd/instance/list.go` (forbidden ⇒ actionable RBAC error on `-A`)
- [x] 4.6 `instance diff`: orphan detection reads `status.inventory` from the CR — `cmd/instance/diff.go` uses `inventory.GetRecord`

## 5. Deletions and cleanup

- [x] 5.1 Delete `internal/inventory/{secret.go,crud.go,list.go}` and the Secret-era paths in consumers; keep `pkg/inventory` + `stale.go` untouched; retain Secret unmarshal only inside the migration path — deleted; unmarshal survives only in `inventory/legacy.go`. Also swept the six `//go:build ignore` integration programs under `tests/integration/{deploy,inst-list,inst-tree,inventory-apply,inventory-ops,module-apply}` (invisible to `go build ./...`) — all rewired to the CR store and compile-checked green
- [x] 5.2 Remove `pkg/ownership` `createdBy`-based enforcement (superseded by 2.1); sweep remaining `createdBy`/Secret-name references — `pkg/ownership` deleted; no `createdBy`/`InstanceInventoryRecord`/`SecretName` references remain
- [x] 5.3 `task check` (fmt, vet, lint, tests) green across the repo — fmt/vet clean, unit tests pass (26 pkgs), lint 0 issues under CI-pinned golangci-lint v2.11.3; `test:integration`/`test:e2e` require a live kind cluster (6.1–6.3)

## 6. Verification and docs

- [x] 6.1 e2e: first apply creates CR with correct spec/status subset; re-apply increments revision; rename prunes; delete removes CR last (kind cluster, CRDs via `opm operator install --crds-only`) — **RAN on kind-opm-dev with the alpha.4 operator installed**: `deploy`, `inventory-apply`, `inventory-ops`, `inst-list` PASS; `inst-tree` passes all 9 inventory steps (apply/discover/revision/prune/resolve-by-name/-uuid), its step 10 fails only on the testdata fixture's CUE `#ModuleInstance` registry resolution (outside the CR change); `module-apply` blocked before its inventory logic by a pre-existing `~/.opm/config.cue` provider-import error in the local registry. Every CR create/read/revision/rename-prune/delete-CR-last path verified live.
- [x] 6.2 e2e: migration — seed a legacy Secret inventory, apply, assert CR ported + Secret deleted + stale entry pruned; assert status/list do not see unmigrated Secrets — `tests/integration/migration/main.go` **PASSED on kind-opm-dev**: legacy Secret (rev 4) ported to a CR at rev 5, cm-stale pruned, Secret deleted after the status write, and a Secret-only instance invisible to GetRecord/ListRecords. Plus cluster-free helper coverage in `internal/inventory/legacy_test.go`.
- [x] 6.3 e2e: gates — missing CRD hint; SSAR denial aborts pre-apply (namespace-scoped user); ceiling inert against current operator release (field absent ⇒ skip) — `tests/integration/gates/main.go` **PASSED on kind-opm-dev**: CRD presence, field floor, ceiling-skip (no Platform), and status-RBAC allowed. Missing-CRD/SSAR-denial refusal paths remain unit-tested (`gates_test.go`).
- [x] 6.4 Update command reference docs + QUICKSTART: CRD prerequisite, `--all-namespaces`, migration note, breaking Secret removal — `QUICKSTART.md` gains the `opm operator install --crds-only` prerequisite step, the `--all-namespaces` list example, and the Secret->CR migration/breaking note; the `instance apply` / `module apply` command help text (source of the generated reference) updated for the CRD prerequisite and de-Secreted.
- [x] 6.5 Record the landing `history` event in `enhancements/0006/config.yaml` (slice `cli/<archive-date>-cli-cr-inventory-backend`) — C1 implementation history event appended (validates under `task enhancements:vet:one ID=0006`); the archive-date `slice:` ref is added at archive time.

## 7. A6 released — pin bumped (ceiling e2e needs a cluster)

- [x] 7.1 After the opm-operator release carrying `Platform.status.operatorVersion` ships: `task operator:sync VERSION=<tag>` pin bump, then e2e the ceiling gate against a real operator (skip-when-absent + refuse-when-newer paths) — **A6 unblocked**: it merged to opm-operator `main` (PR #47) and shipped in releases **v1.0.0-alpha.3** and **v1.0.0-alpha.4** (latest; the Platform CRD carries `status.operatorVersion` at both). Ran `task operator:sync VERSION=v1.0.0-alpha.4` — pin bumped v1.0.0-alpha.2 → v1.0.0-alpha.4, embedded Platform CRD now carries `status.operatorVersion`, ModuleInstance CRD field-floor deps (`spec.owner`, `status.inventory`) intact; build + vet + all unit tests green. **Ceiling e2e VERIFIED on kind-opm-dev**: deployed the alpha.4 operator, applied a `Platform` (`name: cluster`) — the operator reconciled it and stamped `status.operatorVersion: v1.0.0-alpha.4` (`Materialized`). `GateOperatorVersionCeiling` then, against that live Platform: refuses `1.0.0-alpha.3` and `0.9.0` (older) with the upgrade-the-CLI error, passes `1.0.0-alpha.4` (equal) and `2.0.0` (newer), and skips `dev` with a warning. The skip-when-absent path was verified before the Platform existed (gates program). (The A6 OpenSpec change is not yet archived in the opm-operator repo, but the field is merged + released.)
